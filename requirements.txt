# Зависимости проекта «АвтоГрад».
#
# Установка:
#   python -m pip install -r requirements.txt
#
# Требуется Python 3.10+ (используются аннотации вида `str | None`).
# Проверено на 3.14.

# Вызов модели. Провайдер меняется строкой в конфиге (ADR-0005),
# поэтому клиент один и работает с любым OpenAI-совместимым endpoint.
openai>=1.60,<3

# HTTP для Telegram Bot API: long polling без вебхука (ADR-0003).
httpx>=0.27,<1

# Чтения .env отдельной библиотекой нет намеренно: загрузчик написан
# в tools/assistant.py (функция load_env) — десять строк вместо зависимости.
#
# Сборка фикстур и все четыре валидатора работают на стандартной
# библиотеке: python tools/validate_fixtures.py и остальные запускаются
# без единого стороннего пакета.
